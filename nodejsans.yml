---
- hosts: webservers
  tasks:
   - name: Install forever
     npm: name=forever global=yes state=present

   - name: npm installation
     
     command: npm install

   - name: execute npm
     command: forever start /root/SonarQubeNodeJS/app/views/index.html
